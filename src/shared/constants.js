import BN from 'bn.js';

export const KEEPER_HISTORY_DATE_FILTER = {
    LAST_DAY: 'LAST_DAY',
    LAST_WEEK: 'LAST_WEEK',
    LAST_MONTH: 'LAST_MONTH',
}

export const BIG_TEN = new BN(10);
export const BIG_ZERO = new BN(0);
export const CURRENT_BLOCK = 31;